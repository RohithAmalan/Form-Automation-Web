# Server Configuration
PORT=3001
SESSION_SECRET=dev_secret_change_me
DATABASE_URL=postgresql://postgres:password@localhost:5432/form_automation

# AI Configuration (Get key from openrouter.ai)
OPENROUTER_API_KEY=sk-or-v1-your-key-here
# OPENAI_API_KEY=sk-your-openai-key (Optional alternative)

# Google OAuth (Required for "Login with Google")
GOOGLE_CLIENT_ID=your_client_id
GOOGLE_CLIENT_SECRET=your_client_secret
CALLBACK_URL=http://localhost:3001/auth/google/callback

# Admin Credentials (For "System Admin" login)
ADMIN_EMAIL=admin@local
ADMIN_PASSWORD=admin123

# Timeouts & Limits (Optional overrides, defaults are shown)
MAX_RETRIES=2
RETRY_BACKOFF_MS=2000
PAGE_LOAD_TIMEOUT_MS=60000
ELEMENT_WAIT_TIMEOUT_MS=10000
